gunicorn>=21.2.0
cryptography>=42.0
bcrypt>=4.0.0
psycopg2-binary>=2.9
kafka-python>=2.0.2
redis>=5.0.8
PyYAML>=6.0
clickhouse-connect>=0.7
pyarrow>=15.0
boto3>=1.34
google-cloud-logging>=3.10
python-dotenv
lxml
aiohttp==3.13.3
aiohttp_socks==0.11.0
beautifulsoup4==4.14.3
dnspython==2.8.0
Flask==3.1.3
httpx==0.28.1
protobuf==7.34.0
PyJWT==2.11.0
pyOpenSSL==25.3.0
python_nmap==0.7.1
Requests==2.32.5
urllib3==2.6.3
python-whois==0.8.0
APScheduler>=3.10.0
# PDF report generation (Executive + Technical ASM reports)
reportlab>=4.0
matplotlib>=3.7
numpy>=1.24
Pillow>=10.0
psutil>=5.9

# ── ITAM agentless discovery + DNS monitor ────────────────────────────────────
paramiko>=3.0.0          # SSH-based agentless inventory (Linux/macOS targets)
pywinrm>=0.4.3           # WinRM/PowerShell remoting (Windows targets)
dnslib>=0.9.23           # DNS forwarding resolver for network-level Shadow AI detection
pysnmp-lextudio>=4.4.12,<6.0.0  # SNMP v2c polling; <6 keeps synchronous hlapi API
zeroconf>=0.131.0        # mDNS/SSDP passive device discovery
boto3>=1.34.0            # AWS EC2 cloud asset discovery
azure-mgmt-compute>=30.0 # Azure VM cloud asset discovery
azure-mgmt-network>=25.0 # Azure NIC/IP address resolution

# ── Secrets backends (optional — install only what you use) ───────────────────
# Azure Key Vault + Azure Arc Managed Identity
azure-identity>=1.15.0
azure-keyvault-secrets>=4.7.0
# HashiCorp Vault
# hvac>=2.0.0
# Infisical (developer secrets management — also supports Native Azure Auth for Arc)
infisical-python>=2.0.0
